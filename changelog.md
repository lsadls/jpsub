# Changelog

## 2026-09-25 (5)

### 新功能

- **`--hard` 硬核拆句翻译**：`translate` / `text` 加此参数后，每条原文按「。」拆成单句逐句翻译再按原序拼回——单句被审查/翻坏只回退那一句，不连累整条；单句译文也进缓存，下次续翻只重翻失败的单句；请求次数明显增多，适合对付经常翻坏长段的素材。
- **`text` 子命令**：`jpsub text 文件.txt` 逐行发给 AI 翻译，输出 `文件.zh.txt`（`-o` 指定输出），支持 `--comment` / `--glossary` / `--hard`。
- **译文调整器内直接翻译**：新增「续翻 / 重翻 / 硬核」按钮，翻译在后台线程执行、进度实时显示（经共享 LineCapture 收集），完成后自动刷新页面。
- **标为未译（↺）**：某句翻得烂时点时间栏行末 **↺**，该句标回 `[[未译]]` 并删除缓存里的旧译文，之后「续翻」只重翻这句。
- **拒绝语检测（is_junk_tr）**：混入 AI 拒绝语的译文显示原文但标橙，续翻时自动重翻。
- **`glossary/` 项目名词表目录**：按素材存放名词对照表（如 takuya.txt、inm.txt），配合 `--glossary` 使用。

### 改进

- **ASS 折行算法重写**：只数文字不算标点（符号直接挂行尾）；整句优先折行，超长句按句中停顿（，、；：）与引语开口切短；引语前可断行；配对符号开口侧不挂行尾；多行时短行尾部补全角空格补齐等宽。
- **共享进度捕获 `progress.py`**：`LineCapture` 统一接管后台任务的 print/tqdm 输出（去 ANSI 码、进度条压缩成「名称 百分比 (数字)」），主页/编辑器展示行为一致。
- **默认参数调整**：`BATCH_SIZE` 30→10、`MAX_CHARS` 30→20（settings 同步）。
- **缓存**：新增 `remove(src)` 删除条目（配合 ↺ 重翻指定句）。

## 2026-09-24 (4)

### 破坏性重构：全面 segments.json 化（废弃 in/out 简易格式）

- **segments.json 成为唯一数据源**：原文/译文统一存每段的 `text`/`tr`，翻译流程（`ai.translate_file` → `ai.translate_texts`）不再读写 translate-in/out.txt，译文直接写回 segments 并进缓存；`--notrans` 停在 segments.json；删除 `--no-index`。
- **OCR 原始备份 `segments.orig.json`**：每次 OCR 完成后写入（仅原文），旧目录缺失时 `_save_work` 自动补建。
- **译文调整器「还原改动」按钮**：从 `segments.orig.json` 一键撤销所有编辑（译文/增删/时间轴恢复到 OCR 原始状态），确认后刷新页面。编辑器改为直接读写 segments.json（保存时覆盖前仍自动备份），旧目录的 translate-out.txt 打开时自动一次性导入（只补缺不覆盖）。
- **masks.txt → masks.json**：打码清单改为 JSON 数组（{start,end,x,y,w,h,effect}）；旧 masks.txt 仍兼容读取（`mask`/`maskapply` 默认路径自动回退），保存一律写 JSON。
- **续翻/审查降级语义保留**：待翻句=段上无有效 tr 且缓存未命中；`[[未译]]` 占位存进 tr（调整器橙色显示、不进缓存、可重翻）；内容审查拦截自动降级 batch_size=1 重跑；额度不足/中断时已完成部分经 `on_batch` 回调落盘不丢。
- **主页**：「未译」徽章改按 segments 统计；「烧录」按钮检测 masks.json（或旧 masks.txt）；文案同步。
- 旧工作目录无需迁移：translate-out.txt 译文会在 translate/render/edit 时自动导入 segments，旧 in/out 文件保留不删但不再使用。

### 新功能

- **必剪字幕互转**：译文调整器新增「转换」按钮，`.bcc`(必剪 JSON) ↔ `.ass` 按扩展名自动互相转换（pysubs2，样式与 jpsub 默认样式一致），文件在工作目录/output/ 查找。
- **工作目录新布局**：视频与 ASS 移入 `<视频名>.jpsub/` 工作目录内；旧布局（工作目录旁）自动兼容识别，查找时新布局优先。

### 改进

- **译文调整器增强**：原文列也可直接编辑；文本框按内容自适应高度；服务端退出后页面全屏遮罩提示「程序已退出」（/ping 轮询）。
- **`ai.translate_texts` 重写**：签名改为 `(items, ...) -> dict{键:译文}`，新增 `on_batch` 每批回传部分结果（中断时已完成部分不丢），写回 segments 由调用方负责。
- `maskapply` 支持 progress 回调。

## 2026-09-24 (3)

### 新功能

- **浏览器主页**：新增 `home` 子命令，且裸 `jpsub`（无参数）直接打开。页面分四部分（2×2 网格）：① 操作（sm 号下载 / 本地翻译 / 对选中项的编辑·打码·烧录，按钮永不置灰，无效操作弹提示）② output/ 列表（视频与 `<视频>.jpsub` 工作目录合并展示，每 3 秒部分刷新，点击行选中，超 50vh 自动滚动，显示「已生成字幕 / N 条未译」徽章）③ 批量脚本（大型 -s 脚本框（每行一条、# 注释）+「运行脚本」按钮 + 输出显示区（带边框滚动盒、最近 200 行、自动滚到底、tqdm 同样清洗），写临时脚本 output/.home-batch.txt 后台跑 `jpsub -s`）。④ 任务（滚动列表、点击行选中、按钮终止选中任务）。① 面板全按钮化：所有子命令与常用参数组合（下载并翻译/不翻译/引导语降审查/下载烧录、翻译/翻译烧录/仅抽帧OCR/仅关键帧/extract、对选中项的编辑/打码/打码应用/烧录/生成字幕/续翻/进度），经 `/cmd` 通用端点构造命令；「自定义参数」框在 ③ 内、对所有操作生效；说明文字移到页面最下。任务以后台子进程执行（download/render/translate/mask 等仍为独立子进程并行），每条带视频名标识；**run/extract 改为主页进程内队列执行：OCR 模型只在首个用到时加载、任务间复用、队列空闲自动卸载**（进度经 stdout 重定向捕获，终止按钮在下一个 OCR 阶段生效）。端口 8790~8799 自动后移。

### 改进

- **`--force` 参数**：extract/translate 支持，强制重新抽帧 OCR / 忽略已译与缓存全部重翻（默认读缓存跳过）。
- **覆盖前自动备份**：`_backup` 把即将被覆盖的 translate-in/out、segments.json 备份到 `work/backup/<时间戳>/`（extract 还原、render 回写、重翻前触发），防误操作。
- **extract 还原模式（restore_all）**：旧 out 里缺失的键也按缓存/segments 快照回填，把译文完整还原到 out。
- **`maskapply --burn`**：打码后接着把 `<视频>.ass` 烧进结果，一步得到打码+字幕成品。
- **关闭页面即退出**：`edit` 与 `mask` 选取器监听 `pagehide` 发 `/quit`，页面关闭 2s 内无新请求自动结束服务（排除刷新误触），无需 Ctrl+C。
- **移除 `--guide-comment`**：引导语参数从各子命令移除（降审查改由主页按钮组处理）。
- **README 重构**：以「主页」为中心重写使用方法（含 doc/home.png 截图），新增「译文调整」「打码」「批量模式」独立章节与「命令行参考」；settings.py 注释化说明各参数（画质/OCR 模型选择建议等）。

## 2026-09-24 (2)

### 新功能

- **译文调整系统**：新增 `edit` 子命令，浏览器里编辑字幕——每行一条（时间轴+日文原文+中文译文），支持改译文/删除/新增条目，改动实时预览、即写回 translate-out.txt；「生成字幕」按钮等价 `jpsub render`。交互模仿打码选取器（左侧视频预览跳帧，右侧列表，序号点击跳转）。
- **名词对照表（glossary）**：翻译时自动加载 `~/.jpsub/glossary.txt` 与工作目录 `glossary.txt`（`--glossary` 指定文件优先级最高，后加载覆盖同名词条），随系统提示词一次性发送，强制专有名词按对照表翻译。
- **动态批大小**：新配置 `BATCH_CHAR_TARGET`（默认 1500 字符），按待译句平均长度自动收缩批大小（长句博客体自动减句数），避免单批过长导致漏行/截断。
- **下载进度回调**：`download` 支持 quiet 模式并解析 yt-dlp 逐行输出，回调「视频 43% / 音频 88%」双流进度。

### 改进

- **批量模式状态板重写**：每任务固定一行、终端下整块原地重绘（ANSI），任务标签优先取 sm 号/文件名；阶段A子进程通过 Manager 队列回传下载百分比；非终端模式只在阶段变化时打印，避免刷屏；失败打印完整 traceback。
- **批量模式全程静默化**：`download`/抽帧/掩膜/OCR 在批量模式下传 `quiet=True`，散行输出改由状态板统一展示。
- **打码选取器 UI 重排**：左右分栏（左画布+控制，右侧滚动列表，表头吸顶），画布缩小为 `min(46vw,80vh)`，操作提示常驻；上传图片改存临时目录不污染视频目录。
- **模糊效果优化**：`boxblur` 改为只模糊亮度通道（`2:0:0`），强度上限随区域尺寸钳制（约 min(w,h)/5，最大 58），避免大区域模糊发花。
- 翻译支持 `quiet` 模式（批量下不打印 tqdm/警告，由状态板展示）。

### 修复

- `read_meta` 在 segments.json 缺失时返回空 dict 而非报错。
- 掩膜并行的进度条在 quiet 模式下不再输出。

## 2026-09-24

### 新功能

- **打码功能**：新增 `mask` / `maskapply` 子命令。`mask` 在浏览器打开打码选取器，鼠标框选区域、拖动/键盘调时间轴，支持模糊/纯色/图片三种效果，保存为 `masks.txt`；`maskapply` 把 masks.txt 应用到视频输出 `*.masked.mp4`。
- **批量模式**：`jpsub -s <脚本>`，每行一条 jpsub 调用（裸 URL / sm 号自动视为下载）。三阶段流水线调度：下载+抽帧走进程池并行、OCR 单实例按完成顺序排队、翻译渲染线程池并发；支持 `--dl-workers` / `--tr-workers`，任务失败隔离，结束汇总 `批量完成:N/M 成功`。
- **`--guide-comment`**：extract/run/translate/download 及批量脚本每行均支持，追加翻译引导语（委婉措辞），降低敏感内容被 API 审查拦截的概率。
- **内容审查降级**：批量翻译触发审查时自动改用 batch_size=1 逐句重跑，已译句子自动续翻不重发。
- **`[[未译]]` 占位标记**：AI 未返回译文（审查拦截/单批失败/缺行）时在 translate-out.txt 写 `[[未译]]`，一眼看出待人工处理行；标记行不进缓存、不参与渲染、重跑自动重翻。
- **`--start` / `--end`**：跳过视频段，仅截取指定时间区间作为字幕源。
- **日文字幕渲染**：无任何译文时 `render` 自动用日文原文填充生成日文字幕。

### 改进

- **时间轴改为军方时间**（`MMSS` / `HHMMSS`，0.1s 精度）：translate-in/out 的键由纯秒数改为军方时间（`58s` → `0058`），更符合人工编辑直觉；解析兼容带冒号 `11:20` 写法与旧纯秒数键（≥100 按军方时间解读），旧产物自动迁移。
- **下载文件名用视频标题**：下载的视频改用标题（清理非法字符、截断 100 字符、失败回退视频 id）命名，工作目录同步为 `<标题>.jpsub`。
- **掩膜生成多进程并行**：帧掩膜计算走进程池吃满 CPU，附单行刷新进度显示；抽帧 ffmpeg 增加 `-stats`。
- **代码复用重构**：抽出 `_select_keyframes`（抽帧+筛选）、`_export_handoff`（导出+segments 同步）供单视频与批量模式共用；删除已无用的 `NoTextFilter` 链路（`--selected-only` 直接保留全部关键帧）。
- **README**：补充时间轴军方时间写法、打码、批量模式（含为什么用）、cookies 排错等章节。

### 修复

- 旧 out 文件跨运行累积导致编号与原文错位、译文张冠李戴：重排机制改为「编号+原文一致性」校验回填。
- 重复键污染（同一键既有 `[[未译]]` 行又有旧译文）：续翻前先把待重翻键的旧行从 out 剔除，避免重翻键与新行重复。
- 静态水印稀释消失比例导致吞段：掩膜均值剔除静态水印像素。
- 动态噪声背景干扰换段判定：二值化链路改为逐帧自适应亮度门槛。
- OCR 与预览（`--selected-only`）路径掩膜判定不一致导致误杀关键帧：统一筛选逻辑。
